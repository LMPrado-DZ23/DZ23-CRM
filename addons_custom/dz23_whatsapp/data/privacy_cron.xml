<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!-- Retenção e anonimização (ADR-013). -->
    <record id="cron_dz23_privacy_retention" model="ir.cron">
        <field name="name">DZ23: retenção e anonimização (LGPD)</field>
        <field name="model_id" ref="model_dz23_privacy_retention"/>
        <field name="state">code</field>
        <field name="code">model._cron_apply()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="True"/>
    </record>
</odoo>
